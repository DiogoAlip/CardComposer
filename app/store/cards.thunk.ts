import { useCardsStore } from "./cards.store";

export const SwapPrincipalPlayerCards = () => {
    const SwapFrontToBack = useCardsStore((state) => state.SwapFrontToBack);
}

export const startGame = () => {

}

export const turnUpCards = () => {
    
}